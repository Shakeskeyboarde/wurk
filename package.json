{
  "private": true,
  "name": "root",
  "license": "ISC",
  "author": "Chris Ackerman <chris@topher.land>",
  "repository": {
    "type": "git",
    "url": "https://github.com/Shakeskeyboarde/werk"
  },
  "type": "module",
  "scripts": {
    "lint": "eslint --max-warnings=0 .",
    "build": "npm -ws --if-present run build && npm update --silent @werk/cli && werk --clear -p build",
    "test": "npm run lint && npm run build && werk depcheck && werk vitest run --no-build",
    "start": "npm run build && werk --clear",
    "clean": "npm start -- clean",
    "modernize": "npx npm-check-updates@latest -i --workspaces --root --format group"
  },
  "workspaces": [
    "packages/cli",
    "packages/vite-config",
    "packages/command-build",
    "packages/*",
    "packages/test/*",
    "template"
  ],
  "engines": {
    "node": ">=18",
    "npm": ">=9",
    "yarn": "please-use-npm",
    "pnpm": "please-use-npm"
  },
  "engineStrict": true,
  "devDependencies": {
    "@rollup/plugin-typescript": "^11.1.5",
    "@types/eslint": "^8.56.0",
    "@types/node": "^20.10.5",
    "@vitest/coverage-v8": "^1.1.0",
    "@werk/command-build": "*",
    "@werk/command-clean": "*",
    "@werk/command-depcheck": "*",
    "@werk/command-exec": "*",
    "@werk/command-list": "*",
    "@werk/command-publish": "*",
    "@werk/command-run": "*",
    "@werk/command-version": "*",
    "@werk/command-vitest": "*",
    "eslint": "^8.56.0",
    "eslint-config-rational": "^3.0.10",
    "prettier": "^3.1.1",
    "rollup": "^4.9.1",
    "typescript": "^5.3.3",
    "vitest": "^1.1.0"
  },
  "werk": {
    "commands": {
      "a": "typescript",
      "b": "does-not-exist"
    }
  }
}
